# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build golf-os-api`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t golf-os-api`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system golf-os-api && \
          adduser --system -G golf-os-api golf-os-api

COPY dist/apps/golf-os-api golf-os-api/
COPY apps/golf-os-api/package.json golf-os-api/
RUN chown -R golf-os-api:golf-os-api .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix golf-os-api --omit=dev -f install

CMD [ "node", "golf-os-api" ]
